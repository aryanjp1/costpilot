# Postgres password (used by db + backend)
DB_PASSWORD=costpilot_dev

# JWT signing secret — generate with: openssl rand -hex 32
SECRET_KEY=change-me

# Optional: enables LLM-written recommendation summaries
OPENAI_API_KEY=
